module Fib;

global protocol Fib(role A, role B){
    choice at A
    {
        Num1(x1: number) from A to B;
        Num2(y1: number) from A to B;
        Num3(z1: number) from B to A;
        do Fib(A, B);
    }
    or
    {
        Bye() from A to B;
    }
}